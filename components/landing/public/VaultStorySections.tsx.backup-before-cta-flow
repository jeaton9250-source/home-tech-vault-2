"use client";

import Link from "next/link";
import {
  ArrowRight,
  FileText,
  Home,
  Receipt,
  ShieldCheck,
  Wrench,
} from "lucide-react";

import { MARKETING_ROUTES } from "@/lib/marketing/routes";

export default function VaultStorySections() {
  return (
    <>
      {/* REAL LIFE MOMENTS */}
      <section className="bg-[#f5f1e8] px-5 py-20 md:px-8 md:py-28 lg:px-12">
        <div className="mx-auto max-w-[1180px]">
          <div className="mb-12 max-w-[720px]">
            <p className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#617c43]">
              When you actually need it
            </p>

            <h2 className="mt-4 font-serif text-4xl font-medium tracking-[-0.04em] text-[#17212a] sm:text-5xl">
              The useful stuff is already there.
            </h2>
          </div>

          <div className="grid gap-6 lg:grid-cols-3">
            <MomentCard
              icon={Wrench}
              eyebrow="The refrigerator stops cooling"
              title="You already have the model, receipt, warranty, and manual."
              copy="No digging through email. No crawling behind the appliance hoping the model number is still readable."
            />

            <MomentCard
              icon={ShieldCheck}
              eyebrow="Something needs service"
              title="You can see what was done before."
              copy="Keep maintenance notes, service records, dates, documents, and important details with the thing they belong to."
            />

            <MomentCard
              icon={Home}
              eyebrow="Someday, you move"
              title="Your home has a useful record to hand off."
              copy="Instead of rebuilding years of information from memory, you already have the important pieces organized."
            />
          </div>
        </div>
      </section>

      {/* HOME RECORD */}
      <section className="bg-[#fffdf8] px-5 py-20 md:px-8 md:py-28 lg:px-12">
        <div className="mx-auto max-w-[1180px]">
          <div className="grid gap-14 lg:grid-cols-[0.88fr_1.12fr] lg:items-center">
            <div>
              <p className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#617c43]">
                A record of your home
              </p>

              <h2 className="mt-5 max-w-[560px] font-serif text-4xl font-medium leading-[1.04] tracking-[-0.04em] text-[#17212a] sm:text-5xl">
                Keep the story behind the stuff.
              </h2>

              <p className="mt-6 max-w-[570px] text-lg leading-8 text-[#68716c]">
                Not just what you own — the receipt, the warranty, the manual,
                the service history, and the little details that become important later.
              </p>

              <p className="mt-5 max-w-[540px] text-base leading-7 text-[#7c847f]">
                Start with a few important items and let the record grow as you live in your home.
              </p>

              <div className="mt-8">
                <Link
                  href={MARKETING_ROUTES.signup}
                  className="inline-flex min-h-[52px] items-center justify-center gap-2 rounded-full bg-[#183047] px-7 text-sm font-semibold text-white transition hover:bg-[#223e58]"
                >
                  Start My Home Vault
                  <ArrowRight size={16} />
                </Link>
              </div>
            </div>

            {/* VISUAL HOME RECORD */}
            <div className="relative">
              <div className="rounded-[32px] border border-[#17212a]/8 bg-[#f5f1e8] p-5 shadow-[0_30px_80px_-50px_rgba(23,33,42,0.45)] md:p-7">
                <div className="rounded-[24px] bg-[#fffdf8] p-6 ring-1 ring-[#17212a]/6">
                  <div className="flex items-start justify-between gap-5 border-b border-[#17212a]/8 pb-5">
                    <div>
                      <p className="text-[10px] font-semibold uppercase tracking-[0.17em] text-[#617c43]">
                        Kitchen
                      </p>

                      <h3 className="mt-2 font-serif text-3xl text-[#17212a]">
                        Refrigerator
                      </h3>

                      <p className="mt-1 text-sm text-[#7c847f]">
                        Samsung • Added to My Home
                      </p>
                    </div>

                    <div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-[#edf2e7] text-[#617c43]">
                      <Home size={20} />
                    </div>
                  </div>

                  <div className="mt-6 space-y-3">
                    <RecordRow
                      icon={Receipt}
                      label="Purchase receipt"
                      value="Saved"
                    />

                    <RecordRow
                      icon={ShieldCheck}
                      label="Warranty"
                      value="Active"
                    />

                    <RecordRow
                      icon={FileText}
                      label="Manual"
                      value="Ready"
                    />

                    <RecordRow
                      icon={Wrench}
                      label="Last maintenance"
                      value="May 18"
                    />
                  </div>

                  <div className="mt-6 rounded-[18px] bg-[#edf2e7] px-5 py-4">
                    <p className="text-[10px] font-semibold uppercase tracking-[0.15em] text-[#617c43]">
                      Good to know
                    </p>

                    <p className="mt-2 font-serif text-lg leading-7 text-[#40502f]">
                      Everything tied to this appliance stays together.
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* START SIMPLE */}
      <section className="bg-[#183047] px-5 py-20 text-white md:px-8 md:py-24 lg:px-12">
        <div className="mx-auto max-w-[1000px] text-center">
          <p className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#c9d8b7]">
            You do not need to organize everything today
          </p>

          <h2 className="mx-auto mt-5 max-w-[760px] font-serif text-4xl font-medium leading-[1.05] tracking-[-0.04em] sm:text-5xl">
            Start with one thing you&apos;d hate to lose.
          </h2>

          <p className="mx-auto mt-6 max-w-[650px] text-lg leading-8 text-white/70">
            A refrigerator receipt. Your HVAC warranty. The manual for the washer.
            Add one thing now, and let your home record grow naturally.
          </p>

          <div className="mt-9 flex flex-col items-center justify-center gap-3 sm:flex-row">
            <Link
              href={MARKETING_ROUTES.signup}
              className="inline-flex min-h-[52px] items-center justify-center gap-2 rounded-full bg-[#617c43] px-7 text-sm font-semibold text-white transition hover:bg-[#718d4f]"
            >
              Start Free
              <ArrowRight size={16} />
            </Link>

            <Link
              href={MARKETING_ROUTES.demo}
              className="inline-flex min-h-[52px] items-center justify-center rounded-full border border-white/20 px-7 text-sm font-semibold text-white transition hover:bg-white/8"
            >
              See an Example
            </Link>
          </div>
        </div>
      </section>
    </>
  );
}

function MomentCard({
  icon: Icon,
  eyebrow,
  title,
  copy,
}: {
  icon: typeof Home;
  eyebrow: string;
  title: string;
  copy: string;
}) {
  return (
    <article className="rounded-[28px] border border-[#17212a]/8 bg-[#fffdf8] p-7 shadow-[0_20px_60px_-48px_rgba(23,33,42,0.4)]">
      <div className="flex h-11 w-11 items-center justify-center rounded-full bg-[#edf2e7] text-[#617c43]">
        <Icon size={20} />
      </div>

      <p className="mt-6 text-[10px] font-semibold uppercase tracking-[0.15em] text-[#7c847f]">
        {eyebrow}
      </p>

      <h3 className="mt-3 font-serif text-2xl leading-8 text-[#17212a]">
        {title}
      </h3>

      <p className="mt-4 text-[15px] leading-7 text-[#68716c]">
        {copy}
      </p>
    </article>
  );
}

function RecordRow({
  icon: Icon,
  label,
  value,
}: {
  icon: typeof Home;
  label: string;
  value: string;
}) {
  return (
    <div className="flex items-center justify-between gap-4 rounded-[16px] bg-[#f5f1e8] px-4 py-3.5">
      <div className="flex min-w-0 items-center gap-3">
        <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-[#617c43]/10 text-[#617c43]">
          <Icon size={15} />
        </div>

        <span className="text-sm font-medium text-[#59625d]">
          {label}
        </span>
      </div>

      <span className="shrink-0 text-xs font-semibold text-[#617c43]">
        {value}
      </span>
    </div>
  );
}
