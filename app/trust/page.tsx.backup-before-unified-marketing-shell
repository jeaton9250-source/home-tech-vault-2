import PublicMarketingShell from "@/components/landing/public/PublicMarketingShell";
import MarketingLayout from "@/components/marketing/MarketingLayout";
import TrustCenterContent from "@/components/marketing/TrustCenterContent";
import {
  createPageMetadata,
} from "@/lib/marketing/metadata";
import { MARKETING_ROUTES } from "@/lib/marketing/routes";

export const metadata = createPageMetadata({
  title: "Trust Center — Security and Privacy",
  description:
    "Learn how Home Tech Vault protects your household with secure authentication, privacy-first design, role-based sharing, and secure cloud infrastructure.",
  path: MARKETING_ROUTES.trust,
  keywords: [
    "home tech vault security",
    "household data privacy",
    "secure home inventory",
  ],
});

export default function TrustPage() {
  return (
    <PublicMarketingShell>
<MarketingLayout>
      <TrustCenterContent />
    </MarketingLayout>
</PublicMarketingShell>
  );
}
